import 'package:flutter/material.dart';

class Furnitures extends StatelessWidget {
  const Furnitures({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Furniture")),
    );
  }
}
